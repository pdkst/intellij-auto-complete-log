rootProject.name = "auto-complete-log"

pluginManagement {
    repositories {
        maven {
            setUrl("https://maven.aliyun.com/repository/gradle-plugin/")
        }
        maven {
            setUrl("https://maven.aliyun.com/repository/public/")
        }
        gradlePluginPortal()
    }
}
